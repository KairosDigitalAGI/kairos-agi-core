# Contexto operacional — 11/09/2026

Constituição: KAIROS_AGI_BLUEPRINT_V1.md. Adendos: KAIROS_MEMORY_SYNC_V1.md e KAIROS_MEMORY_SYNC_V1_1.md. AGENTS.md define papéis. Não reiniciar a arquitetura.

## Estado atual
- Missão 003 implementada como arquitetura operacional local: oito módulos Clone, Content Brain, pipeline e aprovação versionada. Nenhum serviço externo conectado.
- Dados demonstrativos removidos da interface. Receita, CRM, Coins e métricas externas indisponíveis até conexão real. Contadores editoriais/Clone vêm de registros do navegador.
- Instagram oficial informado: `_kairosdigital_`. Sem autenticação Meta, sem métricas consultadas.
- Clone: `kairos.clone.v1`; editorial: `kairos.editorial.real.v1`. Acervo demonstrativo antigo preservado na chave anterior, fora da interface.
- KAIROS é robô do Founder; Arthur e memórias de cliente continuam isolados.
- Adendo V1.1 inclui Money Hunter, Score Kairos e Ideas Vault como arquitetura futura. Papéis configurados não significam agentes executando.
- Triagem do GitHub: 49 repositórios acessíveis inventariados; metadados, árvores e documentos técnicos selecionados. Não é auditoria integral nem validação de serviços em produção. Relatório detalhado fica em memory/private/repository-audit/REPOSITORY_COMPOSITION.md, fora do Git público.
- Clone não gera voz, rosto, imagem nem vídeo. VideoProvider preparado e desligado. Client Edition não ativada. Vault vazio.
- Nenhuma despesa contratada; outros repositórios e processos de produção não alterados.
- Missão 004: Video Engine cria motion videos do zero a partir de roteiro e também edita vídeo real local. Storyboard, animação, marca, trilha, corte e download WebM rodam no navegador; arquivos e roteiro não são enviados a servidor.
- Integration Control Plane: página e endpoint server-side verificam configuração de Meta, Google e X sem expor valores. O token store e Google OAuth estão configurados em produção; o canal real Kairos Digital (`@KairosDigitalAGI`, ID `UC2TqvTgMsTkywGQS3oiYDsg`) foi conectado em 15/09/2026 e validado pelo endpoint de status. O app Meta e o callback do Instagram foram criados; quatro permissões mínimas estão prontas para teste (perfil, publicação, comentários e Direct). A conexão real ainda depende do segredo em produção e do consentimento da conta profissional. As rotas sociais foram consolidadas; após a Fase 15 (consolidação geral de rotas), o Core usa 3 funções públicas no plano Hobby.

## Consolidação Kairos — 14/09/2026
Founder autorizou consolidar kairos-command, kairos-os e kairos-agi-core num único braço operacional ("usar o que já tem, e se não tiver, criar"). Este Core segue como base (decisão registrada em docs/REUSE_PROVENANCE.md e docs/REPOSITORY_REUSE_STRATEGY.md). Arthur é bot de cliente (Allfix), fora de escopo; KAIROS é o agente WhatsApp pessoal do Founder (`62981554992`, pm2 `kairos`, VPS 2.24.199.205).

Missão 006, Fase 1 entregue: Dashboard lê receita do mês, receita total, MRR e clientes ativos/total do Supabase mestre (schema `command`, mesmo banco do kairos-command) via `api/business-metrics.mjs`; frota de agentes WhatsApp e alertas críticos via `api/agent-status.mjs`. Ambas as rotas são server-side, só leitura, atrás de Basic Auth própria deste projeto e desbloqueadas por sessão. Nenhuma migration/RLS de origem alterada.

Missão 006, Fase 2 entregue: chat consultivo com qualquer um dos 27 agentes do organograma (`AgentsPage` → botão "Conversar"), provider de LLM trocável em `api/_providers/` — Anthropic e OpenAI (planos pagos do Founder) primeiro, OpenRouter só reserva de último caso, nunca default. Nenhum agente tem executor conectado; o chat nunca finge ter agido. Mesma Basic Auth do Painel Operacional.

Missão 006, Fase 3 entregue: schema do Content Engine desenhado em `kairos-command/supabase/migrations/0020_content_engine.sql` (`content_jobs`, `content_assets`, `content_calendar`, `avatars`, `prompt_library`) — **pendente de aplicar em produção** pelo Founder via SQL Editor do Supabase. Neste Core, `api/content-jobs.mjs` já lê o pipeline real e permite ao Founder registrar uma ideia nova (fica em etapa=ideia até o motor de geração existir); `ContentEnginePanel` no Dashboard. Sem a migration aplicada, tudo reporta "indisponível"/503 citando o arquivo pendente — nunca fabrica job. Próximas fases planejadas: motor de geração (roteiro/imagem/vídeo/legenda) e OAuth do YouTube.

Missão 006 — Hunter Skill: investigação encerrada em 14/09/2026 sem mudança neste Core. Não havia consolidação pendente: o Hunter da Sofia (painel em `kairos-command` + motor na VPS da Sofia + ponte HTTP já aplicada em produção) já funciona ponta a ponta; `kairos-hunter-skill` é template não usado e `kairos-leadgen` é produto diferente. Ajuste feito ficou só em `kairos-command` (correção de doc/comentário desatualizado, commit `834cc7d`).

Missão 006, Fase 4 entregue: OAuth do YouTube — primeira integração de publicação ativada de verdade (Instagram/TikTok seguem esperando aprovação da Meta). Cofre de tokens cifrados em `kairos-command/supabase/migrations/0021_integracoes_tokens.sql` (`command.integracoes_tokens`, RLS sem nenhuma policy — só `service_role` lê) — **pendente de aplicar em produção**. Neste Core: `api/_crypto.js` (AES-256-GCM para os tokens, HMAC com janela de 10 min para o `state` do OAuth) e `api/_youtube.js` orquestram consentimento → troca de código → confirmação do canal → gravação cifrada; rotas em `api/integrations/youtube/`; botão "Conectar canal" na `IntegrationsPage`. Sem a migration ou sem o cliente OAuth do Google configurado (`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`GOOGLE_OAUTH_REDIRECT_URI`), tudo falha fechado com o motivo exato. Ativação real depende do Founder criar o OAuth Client Web no Google Cloud Console — fora do que este agente pode fazer sozinho.

Missão 006, Fase 5 entregue: primeiro estágio do motor de geração do Content Engine — roteiro real a partir de uma ideia (`etapa ideia→roteiro`), um clique do Founder por job, nunca em lote. `api/_content.js#generateScript` valida `jobId`, busca o job real (404 se não existe, 409 se a etapa não é `ideia` — nunca regenera silenciosamente), chama o provider pago via `selectProvider` (mesma prioridade Anthropic → OpenAI → OpenRouter da Fase 2; 503 se nenhum estiver configurado), grava o roteiro em `command.content_assets` e avança `content_jobs.etapa` para `roteiro` via `patchCommand` (novo helper em `api/_command.js`). Rota `api/content-jobs/generate-script.mjs`, atrás da mesma Basic Auth; botão "Gerar roteiro" em `ContentEnginePanel` por job em etapa `ideia`. Sem a migration `0020_content_engine.sql` aplicada ou sem provider de LLM configurado, falha fechado citando o motivo exato — nunca finge ter gerado nada. O gate de etapa `aprovacao` fica mais adiante no pipeline (pré-publicação, documentado assim no comentário da própria tabela `content_jobs`) — mas o gate de *gasto*, `content_jobs.aprovado` (booleano separado, comentário em `content_assets.gratuito`), é anterior e obrigatório: corrigido no mesmo dia após auto-revisão, `generateScript` agora recusa com 402 sem `aprovado:true`, e uma ação explícita separada (`approveContentJob`, botão "Aprovar geração paga") liga esse booleano antes de qualquer chance de "Gerar roteiro" aparecer. 10 testes no total entre as duas entregas (`tests/content-engine.test.mjs`), 60/60 passando no Core inteiro.

Missão 006, Fase 6 entregue: geração real de imagem de capa (`etapa roteiro→imagem`) no Content Engine, via OpenAI (`api/_providers/openai.js#generateImage`) — único provider deste Core com esse recurso, sem fallback. Bucket público `content-assets` no Supabase Storage (migration `kairos-command/supabase/migrations/0022_content_assets_bucket.sql`, **pendente de aplicar**); `api/_storage.js` novo sobe o binário via REST puro. `generateImage()` segue a mesma cascata de falha fechada e reaproveita o gate `content_jobs.aprovado` da correção pós-Fase 5 — a aprovação é por job, não por etapa, então um job já aprovado para o roteiro não precisa reaprovar para a imagem. Botão "Gerar imagem" no `ContentEnginePanel`, jobs em etapa=roteiro. 7 testes novos, total 67/67 passando.

Missão 006, Fase 7 entregue: geração real de vídeo (`etapa imagem→video`), motor free-tier primeiro. `api/_providers/veo.js` (Google Veo via AI Studio, `GOOGLE_AI_KEY`, assíncrono com polling) e `api/_providers/fal.js` (fal.ai, fila assíncrona, `FAL_KEY`) novos. `generateVideo({jobId, tier})`: `tier="free"` tenta Veo primeiro, cai pro Kling v1.6 (fal.ai) só se o Veo devolver 429 ou não estiver configurado — mas como esse fallback não é gratuito de verdade (fal.ai cobra por segundo), continua exigindo `aprovado:true` como qualquer outra geração paga, mesmo rotulado "free" no pedido original. `tier="paid"` usa Kling v2.1 Master, sempre exige aprovado. A URL do provider é gravada direto em `content_assets.storage_path` (sem subir pro bucket próprio, diferente da imagem). 13 testes novos, total 79/79 passando.

Missão 006, Fase 8 entregue: publicação real no YouTube (`etapa video→publicado`), fechando o pipeline ponta a ponta pela primeira vez. `api/_youtube.js` ganhou `getValidAccessToken()` (decifra/renova o token da Fase 4 via refresh_token quando vencido) e `uploadVideo()` (multipart real, zero SDK, sobe como privado por padrão). `postToYoutube({jobId})` baixa o vídeo do `content_assets.storage_path`, sobe pro YouTube e grava `content_calendar`. **Desvio deliberado do plano original**: a migration 0020 desenhou `etapa="aprovacao"` como o gate de pré-publicação ("nada publica antes disso"), mas legenda/aprovacao ainda não têm motor — em vez de deixar Fase 8 morta esperando as Fases 9/10, `postToYoutube` reaproveita o mesmo `content_jobs.aprovado` (já usado como gate de gasto desde a correção pós-Fase 5) também como o sinal explícito do Founder para publicar. Documentado em código e aqui para revisitar quando legenda/aprovacao ganharem motor de verdade. 9 testes novos, total 94/94 passando. Continua bloqueado pra ativação real pelas mesmas pendências da Fase 4 (migrations + OAuth Client do Google).

Correção pós-Fase 8 (14/09/2026, mesmo dia): as 3 migrations pendentes (`0020_content_engine.sql`, `0021_integracoes_tokens.sql`, `0022_content_assets_bucket.sql`) foram aplicadas pelo Founder em produção e verificadas via REST direto (5/5 recursos respondendo 200). Os 10 env vars necessários (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `KAIROS_TOKEN_ENCRYPTION_KEY`, `GOOGLE_CLIENT_ID/SECRET`, `GOOGLE_OAUTH_REDIRECT_URI`, `GOOGLE_AI_KEY`, `FAL_KEY`, `KAIROS_USER/PASS`) também foram configurados na Vercel; redeploy feito para o runtime pegar todos. Um brief de missão seguinte trouxe 3 alegações de bug (gate de unlock do Painel Operacional bloqueando o botão do YouTube; contador "OAuth pronto" mostrando 1/2; rota de callback supostamente ausente) que a inspeção direta do código já em produção mostrou serem falsas: o gate é BasicAuth server-side deliberado e compartilhado com dado de receita (mesmo `checkAuth`), o "1/2" é o denominador correto (2 provedores oauth — YouTube + Instagram, Instagram ainda sem aprovação da Meta) e o callback já existe desde a Fase 4. Nenhuma das três foi alterada; reportado ao Founder em vez de "corrigido" às cegas — red line do próprio brief ("dívida técnica crítica se reporta antes de prosseguir").

Missão 006, Fase 9 entregue: Avatar Studio. `command.avatars` (migration 0020) só guarda progresso (nivel/xp/coins/conquistas) por `agente_slug` — identidade continua só em `src/data/agentRegistry.json`, nunca duplicada no banco (mesmo texto da própria migration). `api/_avatars.js#listAvatars` faz o join real (registro completo, com baseline 1/0/0 para quem ainda não tem linha); `ensureAvatar` só cria progresso para um `agente_slug` já existente no registro, nunca uma identidade nova — rejeita slug desconhecido. Rota consolidada `api/avatars/[action].mjs` (`list`/`upsert`), mesmo padrão de `[action].mjs` da Fase 7/8 pra não estourar o teto de Serverless Functions. Nova página `AvatarStudioPage` + entrada no sidebar. Desvio deliberado do brief original: nada de `INSERT` cru com nome/role/personality/story — colidiria com identidades já existentes (ex.: ORION já é CEO no registro) e com colunas que não existem na tabela real. 6 testes novos.

Missão 006, Fase 10 entregue: Story Engine (gamificação). `api/_story.js#generateDailyNarrative` recalcula métricas reais (receita/MRR/clientes de `_business.js`, contagem de jobs de `content_jobs`) e só então pede a um provider pago pra narrar em primeira pessoa, tom vigilância — nunca aceita métrica vinda do cliente (red line "não inventar dados"). `getAgentActivity` lê os `content_jobs` mais recentes e rotula cada um com o departamento dono daquela etapa (`roteiro`→Social, `video`→Video AI, `publicado`→YouTube AI etc.) — rótulo organizacional, não alegação de execução real (nenhum agente tem executor conectado, mesma honestidade da Fase 2). Rota consolidada `api/story/[action].mjs` (`narrative`/`activity`) — com essa, o total de rotas chega a exatamente 12, o teto do plano Hobby. Seção "Operações ativas" nova no Dashboard (`OperationsFeed.tsx`). Desvio deliberado do brief: sem `/api/generate-script.ts` (não existe essa convenção no repo) e sem `content_jobs.approved` (coluna real é `aprovado`). 5 testes novos, total 105/105 passando.

Missão 006, Fase 11 entregue: Instagram OAuth real (token longo cifrado, validação de perfil antes de gravar) sobre o mesmo cofre `command.integracoes_tokens` do YouTube. As quatro rotas físicas do YouTube foram substituídas por `api/integrations/[provider]/[action].mjs`, caindo de 12 para 9 funções públicas no plano Hobby. 110 testes passando.

Missão 006, Fase 12 entregue: publicação real de Reels no Instagram (`etapa video→publicado`), fechando a lacuna que a Fase 11 deixou documentada ("Ela não publica Reels"). `api/_instagram.js` ganhou o fluxo assíncrono de três passos da Content Publishing API (criar container com URL pública → consultar status até `FINISHED` → publicar) e a renovação do próprio access_token (o Instagram não tem `refresh_token` separado do YouTube). `postToInstagram({jobId, caption})` grava o `creation_id` em `content_calendar` (`status:"agendado"`, único valor do enum real que cobre "ainda não publicado") ANTES de esperar o processamento — se a function for encerrada no meio do polling, a próxima chamada retoma do mesmo container em vez de duplicar o Reels. Polling configurável (`INSTAGRAM_POLL_INTERVAL_MS`/`INSTAGRAM_POLL_MAX_TENTATIVAS`, mesma convenção do provider `fal.js`) com orçamento curto; se ainda não terminou, devolve `status:"processando"` sem erro em vez de bloquear a function. Nenhuma rota nova (ação somada ao `[action].mjs` já existente); total continua em 9/12. 10 testes novos, total 122/122 passando; typecheck e build limpos. Ativação real segue bloqueada pelas mesmas pendências da Fase 11 (variáveis Meta + consentimento do Founder).

Missão 006, Fase 13 entregue: link direto para a conta social conectada, respondendo ao pedido do Founder de conseguir ver e clicar para abrir o canal/perfil real por trás de cada integração. `computeYoutubeStatus` monta `profileUrl` a partir do `account_id` (ID real do canal, já gravado desde a Fase 4 — zero chamada nova ao Google); `computeInstagramStatus` monta `profileUrl` a partir de `account_label` só quando ele já é um `@username` (formato gravado pela Fase 11 quando a Graph API devolve `username`), senão devolve `null` em vez de inventar link a partir de um nome de exibição. `IntegrationsPage.tsx` mostra o botão "Ver conta conectada" quando há link, e uma frase explicativa quando não há. X segue sem `profileUrl` nesta fase — não existe conta armazenada nem OAuth para X no Core hoje, lacuna registrada para fase futura. 2 testes novos, total 124/124 passando; typecheck e build limpos.

Ativação operacional do YouTube (15/09/2026): YouTube Data API v3 habilitada no projeto Google Cloud `kairos-495011`, conta de teste autorizada e OAuth concluído para o canal Kairos Digital (`@KairosDigitalAGI`). O endpoint de produção confirmou `connected:true`, `accountLabel:"Kairos Digital"` e o `profileUrl` derivado do channel ID real. A cota padrão gratuita do Google é usada; nenhum plano pago foi habilitado. O Instagram segue como próxima pendência externa: o app Meta e o callback já foram criados e os quatro escopos mínimos (perfil, publicação, comentários, Direct) estão prontos para teste, mas a conexão real ainda depende do segredo em produção e do consentimento da conta profissional.

Missão 006, Fase 14 entregue: Mapa do Projeto, uma página persistente no dashboard (`command.project_log`, migration 0023 pendente de aplicar) que registra o que já foi feito, o que falta e novas ideias sobre o desenvolvimento do próprio Core — qualquer agente (Claude Code, Codex) grava uma entrada ao fim de cada sessão, regra agora em `AGENTS.md`. `GET /api/project-log` é público de propósito (nunca carrega segredo); `POST` exige a Basic Auth do Painel Operacional. `scripts/log-update.mjs` é o comando padrão de escrita; `scripts/seed-project-log.mjs` semeia o histórico real das Fases 1-13 assim que a migration for aplicada. 8 testes novos, total 132/132 passando; typecheck e build limpos. Rota nova (não consolidada), total 10/12 Serverless Functions.

Missão 006, Fase 15 entregue (trabalho noturno autônomo, 23/09/2026): consolidação geral das rotas Vercel — as 10 funções existentes (`agent-chat`, `agent-status`, `business-metrics`, `content-jobs`, `project-log`, `avatars/[action]`, `story/[action]`, `content-jobs/[action]`, `integrations/status`, `integrations/[provider]/[action]`) foram fundidas em 3 arquivos dinâmicos: `api/[route].mjs` (rotas de nível superior sem sub-recurso), `api/[resource]/[action].mjs` (avatars/story/content-jobs) e `api/integrations/[...route].mjs` (status + OAuth por provedor, catch-all). Nenhuma URL pública, método, Basic Auth ou resposta mudou — os callbacks OAuth do YouTube/Instagram continuam no mesmo endereço já cadastrado nos apps dos provedores. `readIntegrationStatus` saiu de `api/integrations/status.mjs` para `api/_integrations.js` (módulo de lógica, seguindo o mesmo padrão dos demais `_*.js`). 19 testes novos de roteador (`tests/top-level-router.test.mjs`, `tests/resource-action-router.test.mjs`, `tests/integrations-router.test.mjs`), total 151/151 passando; typecheck e build limpos. Libera 9 dos 12 slots do plano Hobby para o Estúdio Kairos (Fase 16) e o webhook do Instagram.

## Próxima missão
Missão 005 (KAIROS WhatsApp) permanece planejada. Exige mapear a instância real e seu acesso antes de conectar o Core, sem interromper o processo atual. Ver docs/modules/VIDEO_ENGINE_V0_4.md.

## Adendo visual
Painel transparente sobre escritório 3D contextual, reaproveitado do kairos-os. EmptyState adaptado do kairos-command. Progresso ligado aos registros locais e às aprovações; sem atividade ou ganhos fictícios. Não representa World 0.2 completo.

## Correção do organograma — Kairos OS
Importados os 15 papéis do cadastro data/agents/agents.json do Kairos OS; CEO mapeado para ORION, WhatsApp para KAIROS, Hunter para Hunter AI, preservando sourceId. Acrescentados 12 papéis/especializações documentados no Blueprint e adendos. Total: 27 configurações, não 27 executores ativos.

A nova página Agentes contém busca, departamento, responsabilidades, indicador-alvo, origem, vínculo hierárquico e acesso ao painel relacionado. Os quatro modelos de reunião são exibidos como propostas de agenda, sem automação. Nenhum saldo, cliente ou token foi importado.

Dashboard e World usam o mesmo cadastro operacional. O Dashboard resume seis agentes e oferece acesso explícito ao organograma completo. O World instancia os 27, exibe balão apenas na seleção/hover para reduzir sobreposição e permite selecionar qualquer agente pelo nome. Fechar o inspetor agora oculta o painel.

O código do motor/importador existente na origem é evidência de implementação, não prova de conexão com este Core. Os executores continuam desconectados. A hierarquia dos 15 papéis originais responde a ORION; vínculos dos especialistas dos adendos são organização adotada neste Core, não importação literal do organograma original.
